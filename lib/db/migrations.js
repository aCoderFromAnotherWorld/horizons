/**
 * Runs pending database migrations.
 *
 * The initial schema is currently complete, so this is intentionally a no-op
 * placeholder for later additive migrations.
 */
export function runMigrations() {
  console.log("DB ready");
}
